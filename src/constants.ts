export const Selectors = {
  APP: 'app'
}

export const Config = {
  SCREEN_WIDTH: 640,
  SCREEN_HEIGHT: 360,
  BACKGROUND_COLOR: '#000000'
}

export const Settings = {
  collisionSettings: {
    BROAD_SCAN_RANGE: 90
  }
}

export const Animations = {
  
}